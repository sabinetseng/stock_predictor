# 台股預測系統 期末簡報規劃（4 人／5 人版）

> 對應專案：`stock_predictor`（Django 5.2 + FinMind/yfinance 爬蟲 + LightGBM/XGBoost/TFT）
> 本文件所有數字、檔名、指令皆取自專案真實程式碼，做投影片時可直接引用。

---

## 〇、全組共用：整體故事線（先由第一位講者帶過）

### 一句話定位（開場白）
「用公開免費資料（FinMind、yfinance）建立台股『未來 20 個交易日上漲機率』預測系統，
以 Django 網站呈現從資料管線 → 機器學習 → 決策看板的完整流程。」

### 全景架構圖（建議畫成 PPT 第 2 頁；之後每位講者輪到自己段落時回來點一次）

```
┌──────────┐   ┌──────────┐   ┌───────────┐   ┌────────────┐   ┌────────────┐
│ ① 爬蟲    │ → │ ② 清洗    │ → │ ③ 資料庫   │ → │ ④ 特徵/標籤 │ → │ ⑤ 模型訓練  │
│ FinMind   │   │ 4 類問題  │   │ Postgres  │   │ 32 個特徵   │   │ LGBM/XGB  │
│ yfinance  │   │ NaN 防護  │   │ /MariaDB  │   │ Return20   │   │ /TFT      │
└──────────┘   └──────────┘   └───────────┘   └────────────┘   └─────┬──────┘
                                                                      ↓
                                            ┌────────────────────────────────────┐
                                            │ ⑥ Django 網站：AI 預測／決策看板／   │
                                            │   訓練紀錄／相關性分析／Admin 後台    │
                                            └────────────────────────────────────┘
```

### 六步管線（README 開頭 1~6 點，正好就是簡報章節順序）
1. **爬蟲**（原始資料入庫）：美金匯率（yfinance）、跨市場指數 ^TWII/^SOX/^VIX、股票行情/籌碼/月營收（FinMind）
2. **資料清洗**：`apps/sync/data_cleaning.py`（無效值、重複、邏輯不一致、離群值警告；NaN 雙保險）
3. **特徵工程**：`python manage.py build_features` → 32 個數值特徵
4. **標籤生成**：`python manage.py build_labels` → Return20（MVP／進階雙關卡）
5. **模型訓練**：LightGBM/XGBoost/TFT + AUC/PR-AUC + walk-forward；訓練完成後**自動執行 SHAP 誤導特徵診斷**（`auto_shap=false` 可關閉）
6. **預測與前端**：AI 預測 API + 決策看板 + 相關性分析

### 時間分配建議（以總長 30 分鐘＋QA 為例）
| 版本 | 每人時間 | 建議頁數／人 |
|---|---|---|
| 5 人版 | 5~6 分 | 6~8 張 |
| 4 人版 | 6~8 分 | 7~10 張 |

---

## 一、5 人分工版本（每人一大類）

### 講者 1：系統總覽 ＋ 爬蟲與資料來源

**頁面安排（約 7 頁）**
1. 封面＋組員分工
2. 系統總覽（架構圖＋六步管線）← 全組共用頁
3. 資料來源總表（資料／來源／資料表／關鍵欄位）
4. FinMind 爬蟲細節與免費版限制
5. yfinance 細節＋自動 fallback 機制
6. 增量同步機制（不重抓全部）
7. 銜接頁：「抓回來的資料一定乾淨嗎？→ 下一棒資料清洗」

**必講重點（真實內容）**
- 兩個資料源都不需要 API Token（免費）：FinMind、yfinance
- FinMind：股票行情（Open/High/Low/Close/**還原收盤價**/Volume）、三大法人買賣超（外資/投信）、融資餘額、單月營收
- yfinance：`USDTWD=X` 匯率、`^TWII` 大盤、`^SOX` 費半、`^VIX` 恐慌指數；FinMind 免費版抓不到全部歷史時**自動改抓 `{代碼}.TW`**（`apps/stocks/services.py` 的 fallback）
- ETF 淨值：歷史用 CSV 匯入（`python manage.py import_etf_nav 0050 --csv nav.csv`），之後每日同步自動累積當日淨值
- 增量同步：以資料庫**最後一筆日期 +1 天**為起點抓取（`fetch_stock_price` 等函式），避免每次全量重抓
- 對應資料表：`StockPrice`／`StockChipData`／`StockMonthlyRevenue`／`UsdTwdRate`／`MarketIndexRate`（TWII/SOX/VIX 共用）／`EtfNavData`／`TradingCalendar`（交易日曆）
- 加分小故事：專案放在**中文路徑**時 yfinance 底層 curl 讀不到 CA 憑證、所有下載回傳空值——`settings.py` 開機自動把憑證複製到純 ASCII 暫存路徑並設 `SSL_CERT_FILE`（展示踩坑與解題能力）

**可展示**：`apps/stocks/services.py`（fallback 程式碼片段）、`apps/fx_data/services.py`、`apps/market_data/services.py`、終端機跑同步指令的畫面

**銜接句**：「免費資料源不保證乾淨——NaN、重複、邏輯錯誤都會出現，接下來看資料清洗。」

---

### 講者 2：資料清洗

**頁面安排（約 6 頁）**
1. 為什麼清洗重要：Garbage in, garbage out——髒資料**不會報錯**，而是安靜地讓模型「學到錯的」
2. 四類問題總覽：缺失值／離群值／邏輯不一致／重複資料
3. 每類的處理規則（規則表）
4. IQR 離群值偵測：為什麼 k=3.0 而不是課本的 1.5
5. 寫入邊界雙保險：NaN/±Inf → NULL（PostgreSQL 與 MariaDB 相容）
6. 清洗報告 → `DataSyncLog`（每次同步留紀錄）

**必講重點（真實規則，出自 `apps/sync/data_cleaning.py`）**
- **股價**：價格 ≤ 0 刪除（不可能是真實股價）；`high < low` 刪除（物理不可能，必是資料錯誤）；重複日期**保留最後一筆**（通常是較新的抓取結果）
- **月營收**：營收 ≤ 0 刪除（正常經營不可能）；重複月份保留最後一筆
- **籌碼**：NaN **補 0** 並記錄 `fixed_nan`（與價格不同——籌碼缺一天不影響累計量級）
- **時序（匯率/指數）**：value ≤ 0 刪除、重複日期刪除、IQR 偵測「日變動率」離群值
- IQR 用 **k=3.0**：股價報酬、匯率變動是厚尾分布，用 1.5 會把正常漲跌停誤殺
- **離群值只警告、不自動刪**：真實漲停/跌停、除權息隔日跳空是真的，刪了反而學不到極端行情 → 寫進 `outlier_warnings` 給使用者判斷
- **兩層 NaN 防護**：第 1 層清洗層剔除（價格/營收/時序任一關鍵值 NaN → 整列不寫入）；第 2 層 `clean_defaults()` 在所有 `update_or_create` 前把 NaN/±Inf 統一轉 NULL——因為 PostgreSQL 能存 NaN 但 **MariaDB/MySQL 會直接丟例外**，此層確保兩種資料庫都能穩定寫入（README 6.2）
- `summarize_report()` 產出摘要（例：「清洗前 250 筆，移除/修正 3 筆異常資料，2 筆離群值警告」）寫入 `DataSyncLog.message`

**可展示**：`apps/sync/data_cleaning.py`（`sanitize_val`／`clean_stock_price_rows`／`_iqr_outlier_mask`）、Django Admin 的 DataSyncLog 清洗訊息

**銜接句**：「乾淨的原始資料入庫後，怎麼變成模型看得懂的 X 跟 Y？」

---

### 講者 3：標籤建立 ＋ 特徵工程

**頁面安排（約 8 頁）**
1. 預測目標 Y：Return20 公式（配一張股價曲線標出 t 與 t+20）
2. 標籤版本一：MVP 單門檻（Return20 > 3%）
3. 標籤版本二：**進階雙關卡（預設）**——超額報酬關卡＋95% 停損關卡
4. 為什麼要雙關卡（示意圖：先暴漲又崩跌的走勢）
5. 時間安全：look-ahead bias 防護
6. 特徵總覽：32 個數值特徵、9 大分類表
7. 代表性特徵講解（挑 4~5 個講透，其餘列表帶過）
8. 特徵缺值處理規則

**必講重點（真實定義，出自 `X_Y_VARIABLE_DEFINITION.md`）**
- **Return20 = (P(t+20) − P(t)) / P(t)**：P 用**還原收盤價**（除權息調整，避免配息造成假下跌）；t+20 是往後數第 **20 個交易日**（不是自然日），`LABEL_LOOKAHEAD_TRADING_DAYS = 20`
- **MVP 單門檻**：Return20 > 3% → 標籤 1（`LABEL_RETURN_THRESHOLD = 0.03`）
- **進階雙關卡（預設，`USE_ADVANCED_LABEL_GATE = True`）**：兩條件**同時成立**才標 1——
  ① 超額報酬關卡：個股 Return20 − 大盤(^TWII) Return20 > 門檻
  ② 停損關卡：未來 20 個交易日內，任一日最低價**不曾跌破** t 日收盤 × 95%（`STOP_LOSS_RATIO = 0.95`）
- **為什麼雙關卡**：單看漲幅會把「先暴漲又崩跌」標成好買點；加上停損關卡後，只有「贏過大盤**且**過程不會讓你停損出場」才是 1
- **時間安全（look-ahead bias 防護）**：跨表對齊一律 `merge_asof`（只准用「當天或之前」的資料）；月營收以**公告日**生效而非所屬月份（公告前模型不能偷看）；指數缺日最多前值填補 5 天
- **32 個特徵分類**（日頻 30 個存 `StockFeatureDaily`、月頻 2 個存 `StockFeatureMonthly`）：
  | 分類 | 數量 | 代表 |
  |---|---|---|
  | 技術面 | 4 | `rsi`（14 日 RSI）、`macd`、`ma20_slope`、`ma60_slope` |
  | 技術面進階 | 7 | `bias_ma20` 乖離率、`volatility_20d` 年化波動、`rsi_monthly` 月線 RSI |
  | 籌碼面 | 2 | `institutional_net_buy_ratio`（法人買賣超/20 日成交量）、`margin_usage_ratio` |
  | 籌碼面進階 | 3 | `inst_net_buy_20d`、`inst_accel` 籌碼加速度、`margin_mom` |
  | 相對大盤 | 2 | `excess_return_vs_benchmark`、`beta_vs_benchmark` |
  | 跨市場總經 | 4 | `usd_twd_change_rate`、`sox_change_rate`、`vix_level_percentile`、`vix_change_rate` |
  | 跨市場進階 | 4 | `sox_mom`、`rs_vs_sox`、`usd_twd`、`vix` |
  | ETF 折溢價（僅 ETF 有值） | 4 | `etf_premium_z20`（20 日 z-score，異常溢價＝均值回歸壓力） |
  | 月頻營收 | 2 | `revenue_mom`（MoM）、`revenue_yoy`（YoY） |
- **缺值處理三規則**：①「整欄皆空」＝結構性不可得（ETF 沒月營收、個股沒折溢價）→ 訓練自動剔除並記錄在模型檔 `feature_columns`；②混合個股＋ETF 訓練取「所有標的都有的特徵交集」；③零星缺值**整列捨棄**（保守策略）
- 樹模型（LightGBM/XGBoost）原生支援 NaN 缺值；TFT 要求最新 `seq_len` 個「特徵完整」交易日才能組窗

**可展示**：`X_Y_VARIABLE_DEFINITION.md`（權威定義檔直接截圖）、`python manage.py build_labels`／`build_features` 執行畫面、Admin 的 `StockLabel`／`StockFeatureDaily` 資料

**銜接句**：「X 跟 Y 都準備好了——怎麼訓練？怎麼知道模型沒有作弊？」

---

### 講者 4：模型訓練（條件、限制與防護）

**頁面安排（約 10 頁）**
1. 三種模型：LightGBM／XGBoost／TFT（各自特性一張表）
2. TFT 小科普（VSN 可變選擇網路＋LSTM＋多頭自注意力；樣本＝最近 `seq_len` 個交易日的特徵視窗）
3. 資料切分：60/20/20（訓練/驗證/盲測）＋自選日期模式＋滾動視窗
4. 評估指標：AUC／PR-AUC、`n_repeats` 重複訓練平均值 ± 標準差
5. Walk-forward 回測與穩定性評估
6. 防過擬合四大機制
7. **訓練的條件與限制**（重點頁）
8. **訓練循環圖**（訓練 → 成績單 → SHAP 診斷 → 修正 → 重訓的迴圈；兩種模型各自診斷法；**診斷已納入訓練流程——訓練完成後自動執行**）
9. **數字判讀表**（AUC／PR-AUC／標準差／SHAP 對應的調整動作）＋ 重訓後比對：情況 A（更安全）／情況 B（去噪成功）
10. 過擬合判斷表（良好訊號 vs 危險訊號）

**必講重點（真實設定，出自 `apps/stocks/model_training.py` 與 `MODEL_TUNING_GUIDE.md`）**
- LightGBM（leaf-wise，管 `num_leaves` 15~63）與 XGBoost（level-wise，嚴格限 `max_depth` 3~6）調參邏輯不同；TFT 用 PyTorch、**CPU 可訓練**（`d_model` 預設 32、`dropout` 0.1、內建驗證 AUC 早停）
- **切分**：60% 訓練／20% 驗證／**20% 盲測**（盲測不參與訓練）；「一鍵重訓練」＝同步完資料**後**重算最新 60/20/20（資料成長切分自動右移，模型永遠學到最新）；「自選日期模式」四個日期完全手動（`train_end` 必須早於 `validation_start`，前後端雙重驗證）
- **防過擬合四大機制**：
  ① Early stopping：`EARLY_STOPPING_ROUNDS = 50`（以驗證集 AUC 為準，TFT 本就內建早停）
  ② 超時防護：`TRAIN_TIMEOUT_SECONDS = 1800`（30 分鐘，超時提前結束並寫入訓練 notes）
  ③ 參數範圍前後端雙重限制：`HYPERPARAM_BOUNDS`（例：`n_estimators` 10~2000、TFT `d_model` 8~128、`epochs` 3~200）
  ④ 特徵重要性 Top 20（LightGBM=split、XGBoost=gain）：單一特徵佔比 **≥ 35%** → 顯示「模型可能背下股票代碼」過擬合警告
- **n_repeats 重複訓練**：同一設定重複多次取 AUC 平均 ± 標準差；TFT 的變異來自初始化/dropout/批次順序，標準差天然有意義
- **Walk-forward**：訓練窗逐年擴大、驗證窗逐年後滑，多輪 AUC/PR-AUC 趨勢表；穩定性判定＝**≥ 2 輪、平均 AUC ≥ 0.55、最遠輪相對最初輪衰退 ≤ 0.05** → 呈現「穩定性：足/不足」與「參考價值百分比」
- **條件與限制（這頁一定要講，老師最愛問）**：
  - 驗證集只有單一類別時：AUC/PR-AUC 記 None、**不啟用早停**（寫入訓練 notes 供查驗）
  - 標籤需要未來 20 日資料 → **最新 20 天沒有標籤**（推論只用特徵，預測基準日＝最後一個特徵完整的交易日）
  - 融券等籌碼資料 **T+1 公布** → 當日收盤後跑管線、隔日早上基準日才推進
  - FinMind 免費版歷史長度限制 → fallback yfinance；TFT 未裝 PyTorch 會給清楚安裝指引；CPU 訓練 TFT 慢，先小 `epochs`/`d_model` 試跑
  - 前端防呆：訓練中按鈕 loading 鎖定（避免連點）、一鍵重訓練 confirm() 二次確認
- **過擬合判斷表**（MODEL_TUNING_GUIDE 一節的表直接搬）：訓練/驗證同步下降＝良好；訓練 95% 但驗證只有 55%＝危險；一句話總結「驗證集錯誤率最低、且與訓練集差距最小，才是最佳參數」
- **SHAP 誤導特徵診斷（黃金流程，已納入訓練流程）**：訓練完成後**自動**對驗證區間執行診斷並把報告存回 `ModelTrainingRun.diagnostics`（超參數 `auto_shap=false` 可關閉；TFT 置換重要性時間預算 60 秒，`shap_time_budget` 可調）——首頁「SHAP 誤導特徵診斷」卡、Walk-Forward 表的 SHAP 欄、訓練紀錄表的 SHAP 欄都會顯示。手動指令 `python manage.py diagnose_model 2330 --model-type lightgbm --auto --retrain` 保留給「歷史舊紀錄補診斷」與「帶修正重訓比對」。三類誤導證據對應三種修正：**獨大**（SHAP 佔比 ≥35%）→ 剔除重訓；**方向悖離領域直覺** → 加單調約束；**依賴曲線鋸齒** → 分箱／平滑（人工）。TFT 吃不了 TreeSHAP，用**逐股置換重要性**等價替代。比對結果兩種解讀：**情況 A**（AUC 略降但顯著＝移除虛胖過擬合／洩漏，新模型更安全）與**情況 B**（AUC 提升＝去噪成功）

**可展示**：首頁「訓練設定卡」截圖（模型下拉/超參數/60-20-20/一鍵訓練）、訓練紀錄表（AUC ± 標準差徽章、SHAP 診斷欄、訓練時間台北 UTC+8）、特徵重要性 Top 20 長條圖、Walk-Forward 穩定性卡（含 SHAP 欄）、首頁 SHAP 誤導特徵診斷卡、`MODEL_TUNING_GUIDE.md` 調參表、`diagnose_model` 診斷報告（SHAP 重要性 + 誤導清單 + 情況 A/B 比對）

**銜接句**：「訓練好的模型怎麼讓使用者在網頁上真正用到？最後進入 Django。」

---

### 講者 5：Django 框架 ＋ 資料串接 ＋ 總結

**頁面安排（約 8 頁）**
1. 什麼是 MVT 架構（Model-Template-View）＋對應本專案的檔案位置
2. 6 個 App 職責表
3. 一次請求的生命週期（序列圖或流程箭頭）
4. 資料串接：ORM ↔ 資料庫（.env 的 `DATABASE_URL`、migrations、Admin）
5. 核心 API 清單（一鍵管線／model-info／feature-importance／model-stability／shap-diagnostics／predict）
6. 前後端串接：fetch ＋ CSRF token ＋ localStorage
7. Live demo（或事先錄影備份）
8. 總結＋未來展望（全組一起謝幕）

**必講重點**
- **MVT（Django 稱 MTV）三層對應**：
  - **Model**＝`apps/*/models.py`（ORM 類別對應資料表，如 `StockPrice`、`StockFeatureDaily`、`ModelTrainingRun`；`migrations/` 負責欄位版本控制）
  - **View**＝`views.py`（收請求、回 `JsonResponse`）＋`services.py`／`model_training.py`（業務邏輯與 ML，與 HTTP 解耦）
  - **Template**＝`apps/dashboard/templates/dashboard/index.html`（莫蘭迪綠卡片式 UI）
- **6 個 App 職責**：`dashboard`（首頁與 SOP 流程）、`stocks`（核心：資料/特徵/標籤/訓練/預測）、`fx_data`（匯率）、`market_data`（指數＋交易日曆）、`analysis`（相關性分析）、`sync`（共用清洗與同步紀錄）
- **一次請求的生命週期**（舉「進行 AI 預測」為例）：
  瀏覽器按鈕 → `urls.py` 主路由分發 → `stocks/urls.py` → `views.py` → `model_training.py` 載入「該架構最近一次訓練完成」的模型 → ORM 讀最新特徵 → 回傳機率 JSON → 前端重繪 K 線買賣點＋指標卡
- **核心 API**：
  - `POST /stocks/walkforward-retrain/`（一鍵重訓練：同步→特徵→標籤→訓練，最新 60/20/20）
  - `POST /stocks/full-pipeline-train/`（自選日期一鍵訓練，需帶 `dates`；`dry_run: true` 只預覽不寫入）
  - `GET /stocks/model-info/`（首頁「本次訓練資訊卡」隨模型下拉動態切換）
  - `GET /stocks/feature-importance/`、`GET /stocks/model-stability/`（特徵重要性與穩定性卡片）
  - `GET /stocks/shap-diagnostics/`（最近一次 SHAP 診斷報告：重要性排行、三類誤導證據、修正建議、新舊 AUC 對比）
  - `/stocks/<代碼>/predict/`（AI 預測）、`/analysis/correlation/<代碼>/`（相關性分析）
- **前後端串接細節**：前端 `getCsrfToken()` 從 cookie 讀 `csrftoken` 帶入 POST header（否則被擋）；`localStorage` 記住模型選擇；按鈕統一 `withButtonLoading()` 防連點；伺服器重啟時 `manage.py runserver`
- **同步 vs 非同步**：預設同步觸發訓練（資料量大要等）；`TRAINING_ASYNC=True` 可改 Celery 非同步
- **三顆按鈕對照表**（README 6.1.1 那張表直接搬，評審一看就懂）：一鍵重訓練／自選日期一鍵訓練／進行 AI 預測——只有前兩個是完整管線，「進行 AI 預測」是純推論
- **Admin 後台**：檢查原始資料、`DataSyncLog` 清洗訊息、`ModelTrainingRun` 訓練紀錄

**Demo 流程建議（約 2 分鐘，務必事先錄影備份）**
1. 首頁 SOP 五步按鈕：檢查是否有新資料 → 同步 → 建立特徵 → 建立標籤 → 訓練
2. 開決策看板 `/stocks/2330/`：K 線＋AI 買賣點、動態突破機率 vs 門檻、AI 推論結果卡（六態狀態機：BUY／WAIT／HOLD／SELL／TAKE PROFIT／STOP LOSS）、特徵監控
3. 右欄策略設定：切換模型架構（資訊卡動態切換）、調整買進門檻即時重算
4. 相關性分析 `/analysis/?stock_code=2330`：USD_TWD/TWII/SOX/VIX 四種分析圖

**總結頁素材**：完整管線回顧圖（第 2 頁架構圖回馬槍）＋三個亮點（免費資料源雙保險、進階雙關卡標籤、防過擬合＋SHAP 診斷循環）＋未來展望（例如：付費資料源、GPU 訓練 TFT、自動調參）

---

## 二、4 人分工版本（合併方式）

| 講者 | 負責範圍 | 與 5 人版的差異 |
|---|---|---|
| 1 | **資料工程**：系統總覽＋爬蟲＋資料清洗 | 講者 1＋2 合併；架構圖頁保留，清洗部分收斂成「四類問題＋規則表＋NaN 雙保險」2~3 頁，細節（IQR k 值、DataSyncLog）口頭帶過 |
| 2 | **標籤＋特徵工程** | 同 5 人版講者 3（內容不變） |
| 3 | **模型訓練**（條件、限制與防護） | 同 5 人版講者 4（內容不變，含 SHAP 診斷循環） |
| 4 | **Django＋Demo＋總結** | 同 5 人版講者 5；開場架構圖改由講者 1 講，講者 4 直接從 MVT 開始 |

> 4 人版每人約 7 分鐘；建議把「頁面安排」中的第 4~6 頁合併成 1~2 頁大表，用口頭補充細節。

---

## 三、共用素材清單（做 PPT 前先備齊）

1. 全景架構圖（依本文件 〇-2 重畫成方塊圖）
2. 六步管線流程圖（橫向箭頭）
3. 資料來源總表（README「原始資料層」表格直接搬：資料／資料表／欄位）
4. Return20 示意圖（股價曲線標 t、t+20，畫一條 95% 停損虛線）
5. 32 特徵分類表（`X_Y_VARIABLE_DEFINITION.md` 的表直接截）
6. 60/20/20 時間軸圖（三段色塊＋「滾動右移」箭頭）
7. 訓練循環圖（AUC→PR-AUC→SHAP 診斷→修正→重訓的迴圈箭頭圖；`make_ppt_v2.py` 第 18 頁即為成品，可直接沿用）
8. 系統截圖包：首頁 SOP 按鈕列、訓練設定卡、訓練紀錄表、特徵重要性 Top 20、Walk-Forward 穩定性卡、決策看板、相關性分析、Admin 的 DataSyncLog、`diagnose_model` SHAP 診斷報告
9. Demo 影片（現場環境出問題時的保險）

## 四、預期 Q&A（練習時互相考問）

| 問題 | 建議回答要點 |
|---|---|
| 為什麼預測目標是 20 個交易日？ | 對應月頻節奏（月營收/月報酬）；參數可調（`LABEL_LOOKAHEAD_TRADING_DAYS`），20 日讓樣本數與訊號強度取得平衡 |
| 離群值為什麼只警告不刪？ | 漲跌停/除權息跳空是真實事件，自動刪除會誤刪合理資料、學不到極端行情；故寫入警告交人工判斷 |
| 標籤正負樣本不平衡怎麼辦？ | 評估用 PR-AUC 補 AUC 不足；進階雙關卡讓標籤更嚴謹；驗證集單一類別時誠實記 None 不硬算 |
| 怎麼防止 look-ahead bias（未來函數）？ | `merge_asof` 只准用當天或之前資料、月營收以公告日生效、保留 20% 盲測區、walk-forward 時間順序回測 |
| AUC 多少算可用？ | 穩定性判定：≥ 2 輪、平均 AUC ≥ 0.55、衰退 ≤ 0.05；且訓練/驗證差距要小（±0.03 內） |
| SHAP 說某些特徵在誤導模型，怎麼處理？ | 三類對症下藥：獨大（≥35%）→ 剔除重訓；方向悖離領域直覺 → 加單調約束；依賴曲線鋸齒 → 分箱／平滑。重訓後比對 AUC：情況 A（略降但更安全）或情況 B（提升去噪成功），兩者都是有用的結論 |
| 網頁會建議停損嗎？系統知道我的持股成本嗎？ | 賣點與停利/停損是「模型訊號的模擬部位」：以買訊當天收盤價模擬進場，報酬達 +6% 停利或 −4.5% 停損就記賣點（決策看板 AI 推論結果卡六態會顯示 SELL／TAKE PROFIT／STOP LOSS）；用途是評估訊號品質。系統**不儲存個人持股成本**，不會針對真實部位建議進出場 |
| 資料能部署在不同同學的資料庫嗎？ | 支援 PostgreSQL 與 MariaDB/MySQL 雙後端；NaN 寫入邊界 `clean_defaults()` 轉 NULL，兩邊都穩定 |
| ETF 和個股為何能一起訓練？ | 「特徵交集」自動剔除結構性缺欄（ETF 無營收、個股無折溢價）；樹模型原生支援 NaN 缺值 |
| 模型多久要重訓一次？ | 一鍵重訓練：同步後重算最新 60/20/20（自動右移）；可掛 Windows 排程/cron 跑 `walkforward_retrain` 每日/每週自動滾動 |
| 網頁預測用的是哪個模型？ | 「該架構最近一次訓練完成」的模型（`predict()` 自動載入）；三種架構各自獨立版本與訓練紀錄 |

---

## 五、製作與排練建議

1. **先共做出「架構圖＋六步管線」兩頁**——這兩頁是全組的骨架，每個人開場都回來點一次，評審才不會斷線
2. 每人結尾都要有「**銜接句**」（上面各段已附），下一棒直接接上
3. 數字講出來加分：32 個特徵、3 種模型、60/20/20、early stopping 50 輪、1800 秒超時、AUC ≥ 0.55、95% 停損線、k=3.0 IQR、SHAP 獨大門檻 35%、實測案例（LightGBM 加約束 0.4376→0.5210、TFT 剔除特徵 0.6107→0.5919）
4. 排練至少一次「接龍」：計時、練銜接句、練 Q&A 互相考問
5. Demo 一定要錄影備份；終端機指令（build_labels/build_features）也先錄