# 小白名詞手冊（股票 × AI 模型超淺白解釋）

> 給完全沒碰過股票與 AI 的人看的字典：每個詞用「一句話＋生活比喻」解釋，全部條列。
> 數字與預設值皆取自本專案程式碼（`apps/stocks/model_training.py`、`stock_predictor/settings.py`），與 `README.md`／`MODEL_TUNING_GUIDE.md`／`X_Y_VARIABLE_DEFINITION.md` 一致。
> 用法：看到不懂的單字 → **Ctrl+F 搜尋**該單字即可。

## 目錄

- 一、股市單字：1-1 股價與交易｜1-2 技術指標｜1-3 籌碼面｜1-4 基本面｜1-5 跨市場總經｜1-6 ETF｜1-7 統計
- 二、AI／機器學習單字：2-1 基本概念｜2-2 切分與防作弊｜2-3 過擬合專區｜2-4 評估指標｜2-5 誤導特徵診斷（SHAP 黃金流程）
- 三、本專案三種模型：LightGBM｜XGBoost｜TFT＋比較表
- 四、訓練參數逐個解釋：這是什麼／調大調小會怎樣／建議值
- 五、怎麼判斷數字有沒有參考價值（AUC 對照表＋對症下藥表＋5-8 訓練循環圖＋5-9 數字誕生過程）
- 六、網頁畫面上會看到的詞
- 七、一頁速查

---

# 一、股市單字

## 1-1 股價與交易

- **股票**：一張「公司的所有權證明」。買股票＝當公司小股東；公司賺錢可分股利，股價漲可賺差價。
- **個股**：單一一家公司的股票（相對於大盤或 ETF）。
- **股票代碼**：股票的編號，像身分證字號。2330＝台積電；0050＝元大台灣 50（ETF）。
- **開盤價**：當天第一筆成交價（台股早上 9:00 開門的第一聲）。
- **收盤價**：當天最後一筆成交價。**本專案訓練用的價格就是收盤價。**
- **最高價／最低價**：當天盤中成交價的天花板／地板（本專案用最低價算「停損關卡」）。
- **成交量（volume）**：今天總共換手多少張。越大＝越多人搶著交易。
- **K 線**：一根蠟燭同時畫出「開、收、最高、最低」四個價，看盤軟體上的紅綠柱子就是它。
- **除權息**：公司發股利／股票給股東那天，股價會自動往下扣——錢從左口袋搬到右口袋，你沒虧錢，但曲線出現假跳水。
- **還原收盤價（adjusted close）**：把除權息的假跳水修回去之後的收盤價。**本專案算報酬一律用這個**，避免配息被誤判成崩跌。
- **漲跌幅**：今天比某天漲跌多少 %。
- **報酬率**：(賣價 − 買價) ÷ 買價 × 100%。100 買進、108 賣出＝8%。
- **交易日**：股市有開門的日子（台股週一～五扣掉假日），一年約 250 天。**20 個交易日 ≈ 一個月**——本專案預測的就是「往後 20 個交易日」。
- **大盤／加權指數（^TWII）**：整個台股市場的平均成績單。個股 vs 大盤＝你考得比全班平均好還是差。
- **滾動／滑動視窗**：計算時只取「最近 N 天」一小段資料，時間一天天往前滑（例如「近 20 日平均量」）。

## 1-2 技術指標（把價格與成交量算成訊號）

- **均線 MA（Moving Average）**：最近 N 天收盤價的平均連成一條線＝「最近 N 天的班級平均分數」，用來看趨勢方向。股價在均線上＝近期買的人平均有賺。
- **MA20／MA60**：20 日均線（俗稱月線）／60 日均線（俗稱季線）。
- **ma20_slope／ma60_slope（均線斜率）**：均線是上坡還是下坡。正值＝往上爬（多頭）、負值＝往下走（空頭）。
- **RSI（相對強弱指標）**：0～100 的溫度計，量「最近買方 vs 賣方誰贏」。本專案用 14 日版。
  - 大於 70＝超買（買太熱，可能回檔）；小於 30＝超賣（賣太冷，可能反彈）。
- **rsi_diff_3d**：RSI 最近 3 天的變化量（動能正在轉強還是轉弱）。
- **rsi_monthly**：月線等級的 14 個月 RSI（看中期是否過熱／過冷）。
- **EMA（指數移動平均）**：進階版均線——越近的日子權重越大，更「記得」最近發生的事。
- **MACD**：快線（EMA12）減慢線（EMA26）的差距，畫成柱狀圖。正值＝多頭動能、負值＝空頭動能，像汽車油門／煞車的力道表。
- **乖離率（bias_ma20／bias_ma60）**：股價離均線有多遠（%）。正太大＝漲太急、負太大＝跌太急——橡皮筋拉太開會想彈回。
- **波動率（volatility_20d）**：股價上下劇烈的程度。本專案＝近 20 日「年化波動率」。大＝雲霄飛車，小＝散步。
- **年化**：把任意期間的數字換算成「一年等於多少」的講法，方便不同期間互相比。
- **vol_ratio_5_20（量能比）**：近 5 日平均量 ÷ 近 20 日平均量。大於 1＝放量變熱鬧；小於 1＝縮量變冷清。
- **Beta（beta_vs_benchmark）**：大盤動 1%，這支平均跟著動幾 %。1.5＝放大鏡（漲跌都更兇）；0.5＝減震器。
- **超額報酬（excess_return_vs_benchmark）**：個股報酬 − 大盤報酬。正＝跑贏市場。
- **sox_mom**：費半近 20 日累積漲跌幅（半導體族群現在強不強）。
- **rs_vs_sox**：個股近 20 日漲跌 − 費半近 20 日漲跌＝你是否比「半導體同學」強。

## 1-3 籌碼面（誰在買？誰在賣？）

- **籌碼**：股票的江湖說法。「籌碼面」＝分析股票現在落在誰手裡。
- **法人**：法人機構＝公司型態的專業投資者（相對於一般散戶）。
- **三大法人**：外資、投信、自營商三種法人。
- **外資**：國外來的法人資金（例如摩根、高盛），台股最大勢力，動向常被當風向球。
- **投信**：台灣本土的基金公司，募集大家的錢去買股票。
- **自營商**：證券商用自己的錢操作。
- **買賣超**：買的量 − 賣的量。買超＝淨買進（搶進）；賣超＝淨賣出（出貨）。
- **institutional_net_buy_ratio**：法人買賣超 ÷ 近 20 日成交量。佔比越高＝法人越用力。
- **inst_net_buy_20d**：外資＋投信最近 20 天「累計」淨買多少（籌碼動能）。
- **inst_accel（籌碼加速度）**：近 5 日日均買超 ÷ 近 20 日日均買超。大於 1＝最近買得比過去更兇（加速中）。
- **融資**：散戶「跟券商借錢」買股票。融資餘額＝全市場借錢買股的總張數。升高＝散戶熱情（但槓桿高，大跌時會被迫賣）。
- **margin_mom**：融資餘額的月變化率（MoM）。
- **資券使用率（margin_usage_ratio）**：融資額度用掉幾 %。太高＝借到快滿，追繳（斷頭）風險高。

## 1-4 基本面（公司賺不賺錢）

- **月營收**：台灣上市公司每月 10 日前要公布「上個月賣了多少錢」。每月更新，是「最快的基本面數字」。
- **MoM 月增率（revenue_mom）**：這個月比上個月成長多少 %（短期動能）。
- **YoY 年增率（revenue_yoy）**：這個月比去年同月成長多少 %（長期趨勢）。
- **公告日 vs 所屬月**：本專案用「公告日」生效——公告前模型不能偷看該月營收（防作弊，見 2-2）。

## 1-5 跨市場與總經（外面的世界）

- **USD/TWD（美元兌台幣匯率）**：1 美金換多少台幣。數字變大＝台幣貶值→對電子出口商帳面有利，也牽動外資錢進錢出。
- **usd_twd_change_rate**：匯率的月變動率；**usd_twd**＝對齊交易日後的匯率值。
- **SOX（費城半導體指數，俗稱費半）**：美國半導體大廠組成的指數。台股電子股跟它連動高＝「昨夜全球半導體的成績單」。
- **sox_change_rate**：費半的月變動率。
- **VIX（恐慌指數）**：美股市場「預期波動」的指數。數字高＝大家很害怕（通常股市正要或正在跌）。
- **vix**：對齊交易日後的 VIX 值；**vix_change_rate**＝VIX 的月變動率（恐懼升溫或降溫）。
- **vix_level_percentile**：現在的 VIX 在歷史上排第幾高（百分位）。90＝比歷史上 90% 的日子都恐慌。
- **^TWII／^SOX／^VIX／TWD=X**：本專案從 Yahoo Finance（yfinance）抓資料用的代碼。

## 1-6 ETF 專用

- **ETF**：一籃子股票打包成一支基金，可以像股票一樣買賣。買 0050≈一次買進台股市值最大的 50 家公司。
- **淨值（NAV）**：ETF 背後那一籃子股票的「真實每單位價值」。
- **溢價／折價**：市價 vs 淨值的差。市價 > 淨值＝溢價（買貴了）；市價 < 淨值＝折價（撿到便宜）。
- **etf_premium_discount**：折溢價率 (%)＝(收盤價 − 淨值) ÷ 淨值 × 100。正＝溢價、負＝折價。
- **etf_premium_ma5**：折溢價率近 5 日平均（近期溢價趨勢）。
- **etf_premium_z20**：現在折溢價偏離近 20 日常態「幾個標準差」。大幅正值＝異常溢價，之後可能回跌。
- **etf_premium_chg_5d**：折溢價率跟 5 天前比變化多少（溢價在擴大還是收斂）。
- **注意**：這 4 個特徵只有 ETF 有值；訓練個股時整欄皆空會被自動剔除。

## 1-7 統計小單字

- **平均值**：全部加起來除以個數。
- **標準差（std, σ）**：數字們「散開的程度」。越小＝每次結果越一致。
- **± 標準差**：重複訓練多次後顯示「AUC 0.58 ± 0.02」＝大部分落在 0.56～0.60（很穩）；±0.10＝很看運氣。
- **百分位（percentile）**：在全部歷史裡排第幾名（%）。
- **z-score**：偏離平均值「幾個標準差」。0＝剛好在平均；+2＝比平常高很多。
- **相關係數（correlation）**：兩組數字同向／反向的程度，範圍 −1～+1。+0.8＝高度同向；−0.8＝高度反向；0＝沒什麼關係。
- **p 值（p-value）**：「這結果是巧合」的機率。慣例上 < 0.05＝大致不是巧合，值得參考。

---

# 二、AI／機器學習單字

## 2-1 模型與訓練的基本概念

- **AI 模型（model）**：一段會「從例子學規律」的數學程式。餵它歷史資料（X）跟答案（Y），它學出「什麼狀況之後容易漲」。
- **訓練（train）**：讓模型做「歷史考古題」並不斷修正內部參數的過程。
- **推論／預測（predict／inference）**：訓練完之後拿新資料去問模型答案。像上考場。
- **特徵（feature，X）**：給模型的「線索」。本專案共 32 個（RSI、法人買超、匯率變化…），全列在 4-6。
- **標籤（label，Y）**：標準答案。本專案 Y＝「往後 20 個交易日有沒有達標」的 1／0（定義見 4-6）。
- **樣本（sample）**：一列資料＝「某天、某支股票的 X + Y」。一次訓練通常用幾千～幾萬列。
- **二元分類（binary classification）**：答案只有兩種的預測題（1＝好買點、0＝不是）。本專案就是二元分類。
- **機率輸出**：模型回傳 0～1 的數字，例如 0.72＝「模型認為 72% 機率是好買點」。
- **模型參數**：訓練中模型自己學出來的內部數值（你不用管）。
- **超參數（hyperparameter）**：訓練「開始前」由你設定的旋鈕（樹幾棵、學多快…）。**你在網頁上調的就是這個**，詳見第四節。
- **損失函數（loss）**：衡量「模型錯得多慘」的分數。訓練＝不斷把 loss 壓低。分類題用 LogLoss。
- **決策樹（decision tree）**：一連串是非題組成的判斷樹（「RSI>70？→法人買超嗎？→…」），像玩 20 個問題遊戲。
- **梯度提升樹（GBDT）**：LightGBM／XGBoost 的家族。白話：一棵一棵種樹，每棵新樹專注修正「前面所有樹合起來還錯的地方」，最後一起投票。
- **神經網路（neural network）**：模仿腦神經的數學結構，把數字一層層轉換、越轉越接近答案。
- **深度學習（deep learning）**：很多層的神經網路。TFT 屬於這類。

## 2-2 資料切分與防作弊

- **訓練集（train）**：模型上課用的課本。本專案用前 60% 的時間。
- **驗證集（validation）**：模擬考。訓練中與調參時用來檢驗「有沒有真的學會」。中間 20%。
- **盲測集（test／holdout）**：指考。最後 20%，訓練與調參完全不碰，最後才驗收。
- **60/20/20 切分**：把資料「按時間順序」切成上述三份。**一定要按時間切**（舊的當課本、新的當考卷），不能隨機抽——否則等於考卷混入課本題目。
- **一鍵重訓練**：本專案按鈕。同步→特徵→標籤→以「目前最新資料的 60/20/20」重訓，切分線自動右移，模型永遠學到最新行情。
- **滾動視窗（lookback_years）**：只用最近 N 年資料（預設 3 年），舊資料自動滑出視窗；設 0＝全部歷史都要。
- **look-ahead bias（前瞻偏誤，俗稱偷看未來）**：模型偷看「當下不可能知道」的資料，導致回測超強、上線就死。本專案三道防線：
  - 跨表對齊只用「當天或之前」的資料（merge_asof）。
  - 月營收以公告日生效（公告前不能偷看）。
  - 標籤雖用未來 20 日，但樣本的特徵全部是「當天以前」的。
- **NaN（缺值）**：Not a Number，資料缺席。本專案在清洗層就剔除；某特徵整欄皆空（如 ETF 沒有月營收）會自動剔除該特徵。
- **特徵工程（feature engineering）**：把原始資料加工成特徵的過程（算 RSI、買超比例…）＝網頁上的「建立特徵」。

## 2-3 過擬合專區（最重要的觀念）

- **過擬合（overfitting）**：模型把考古題「逐題背下來」——考古題 95 分，新考卷不及格。**調參的一切努力都是為了防它。**
- **症狀**：訓練 AUC 很高（如 0.90）、驗證 AUC 很低（如 0.55），兩條線越走越開。
- **健康**：訓練與驗證同步進步，差距小（< 0.05 算健康）。
- **泛化（generalization）**：對「沒看過的資料」的表現能力。我們最終只要這個，不要背書機器。
- **正則化（regularization）**：所有「防背書」手段的統稱——限制複雜度、隨機抽樣、dropout、L1/L2 懲罰等。
- **L1／L2 正則化**：對特徵影響力「課稅」。L2 讓所有權重變小、L1 把沒用的砍到 0（本專案樹模型未開放此旋鈕，系統內建處理）。
- **子抽樣（bagging_fraction／subsample）**：每棵樹只看 80% 資料、80% 特徵→每棵樹看到的世界都不同→只能學共通規律。
  - 本專案特別設 0.8 而非 1.0：沒有隨機性時，重複訓練算出的標準差是「假的 0」，會讓你誤判模型超穩。
- **dropout**：TFT 專用防背書——訓練時隨機關掉部分神經元，逼模型不依賴單一神經元。
- **早停（early stopping）**：驗證分數一段時間沒進步就自動停訓。
  - 樹模型：50 輪沒進步就停（`EARLY_STOPPING_ROUNDS = 50`），所以 n_estimators 只是「上限」。
  - TFT：驗證 AUC 連續數輪沒進步就停，epochs 只是上限。
- **Walk-Forward 回測**：把時間軸切成好幾段，輪流「用過去練、用下一段考」，模擬真實上線情境，看模型會不會一上場就衰退。首頁「模型穩定性評估」卡就是它。

## 2-4 評估指標（成績單怎麼看）

- **準確率（accuracy）**：答對幾 %。看似直覺，但「好買點很少」時會失真（全部答 0 也有 80 分），本專案不以此為主。
- **精確率（precision）**：模型喊買的裡面，真的好的佔幾 %。「喊 10 次對 6 次」＝0.6。
- **召回率（recall）**：全部好買點裡，模型抓到了幾 %（抓漏能力）。
- **AUC**：模型「把好樣本排在前面」的能力，0.5＝擲硬幣、1.0＝完美。**本專案的主要成績單**，詳細判讀見第五節。
- **PR-AUC**：更嚴格的版本，適合「好樣本很少」的情境（本專案正是）。至少要高於「標籤 1 的佔比」才有用。
- **類別不平衡（class imbalance）**：標籤 1 很少、0 很多（好買點本來就少）。三種模型都會自動把少數樣本加權：
  - LightGBM 用 `is_unbalance`、XGBoost 用 `scale_pos_weight`、TFT 用 `pos_weight`，不需要額外補資料。
- **n_repeats（重複訓練次數）**：同一設定重複訓練 N 次（1～10），回報「平均 ± 標準差」。因為子抽樣／隨機初始化，每次結果會略不同——標準差小＝可信。**統計上建議 >1（例如 3～5）**。
- **best_iteration**：早停時「其實第幾棵樹就到了最佳」，記錄在訓練 notes。

## 2-5 誤導特徵診斷（SHAP 黃金流程：訓練 → 診斷 → 修正 → 重訓）

- **特徵重要性 vs SHAP**：重要性只說「模型用了誰」；SHAP 更進一步說「每個特徵把機率推高／拉低多少、方向對不對」——回答「模型會不會被特徵誤導」。
- **SHAP 值**：某個特徵讓這次預測機率「往上抬／往下壓」的貢獻量。全部特徵的 SHAP 加起來 ≈ 模型輸出；取平均絕對值 → 該特徵對模型整體的影響力。
- **TreeSHAP（樹模型專用）**：LightGBM／XGBoost 可直接算的 SHAP，精確且快。
- **置換重要性（TFT 的等價替代）**：TFT 是 PyTorch 神經網路，吃不了 TreeSHAP——改用「把某特徵的值逐股打亂，看驗證 AUC 掉多少」。掉越多＝模型越依賴它。
- **獨大／作弊特徵（leak）**：單一特徵 SHAP 佔比 ≥ 35%（與首頁過擬合警語同門檻）→ 可能「背下股票代碼」而非學通用規律 → **剔除後重訓**。
- **方向悖離（direction）**：SHAP 方向跟領域直覺相反（例如「法人買超越大」SHAP 卻把機率壓低）→ **加單調約束**（monotone constraint）強迫模型順著直覺走。
- **依賴曲線鋸齒（jagged）**：把特徵值分箱畫「SHAP vs 特徵值」曲線，若像鋸齒＝模型在硬背噪訊 → **分箱／平滑／對數轉換**（需人工處理，無法自動修）。
- **情況 A／B（重訓後比對）**：
  - 情況 A：AUC 略降但顯著 → 成功移除過擬合／洩漏，舊高分成績是虛胖，新模型線下稍低、線上更穩更安全。
  - 情況 B：AUC 顯著提升 → 去噪成功，模型真的變強。
  - 變化落在 ±（舊標準差＋新標準差）內 → 不足以判斷，需更多證據。
- **執行**：`python manage.py diagnose_model 2330 --model-type lightgbm --auto --retrain`（詳見 MODEL_TUNING_GUIDE.md 第八節）。
- **自動診斷（現況）**：SHAP 診斷**已納入訓練流程**——每次訓練完成後自動對驗證區間執行並存進 `ModelTrainingRun.diagnostics`（超參數 `auto_shap=false` 可關閉；TFT 置換重要性時間預算 60 秒，`shap_time_budget` 可調）。自動診斷不重訓、不做新舊比對；手動指令保留給「歷史舊紀錄補診斷」與「帶修正重訓比對」兩種情境。

---

# 三、本專案的三種模型（白話版）

## 3-1 LightGBM（梯度提升樹；CLI／API 訓練的預設）

- **一句話**：一隊「快速改錯的小樹」，一棵棵接力修正前面的錯誤。
- **特點**：
  - 速度快、效果穩，還會回報「特徵重要性排行榜」（可解釋性高）。
  - 長樹方式 leaf-wise：每次挑「對降低錯誤最有幫助的葉子」優先生長→管複雜度的旋鈕是 **num_leaves**（葉子數），不是樹深。
  - 標籤不平衡自動處理（is_unbalance）。
- **適合**：日常快速迭代、想看懂「模型在看哪些線索」。

## 3-2 XGBoost（梯度提升樹；對照組）

- **一句話**：同家族的「老牌學長」，長樹方式 level-wise（一層一層長），個性保守、對參數敏感。
- **特點**：
  - 管複雜度的旋鈕是 **max_depth**（樹深），建議比 LightGBM 嚴格（3～6）。
  - 常被當作 LightGBM 的「第二意見」：兩者都說會漲，訊號更可信。
- **適合**：與 LightGBM 交叉驗證觀點。

## 3-3 TFT（Temporal Fusion Transformer；網頁前端預設）

- **一句話**：會「看盤 30 天」的 AI——把最近 30 個交易日的所有特徵當一部小電影看完再下判斷。
- **內部四步**：
  1. **VSN**（Variable Selection Network）：自動判斷「這幾天該重視哪些特徵」。
  2. **LSTM**：有記憶的神經網路，抓出時間序列的節奏（趨勢、轉折）。
  3. **多頭自注意力**：讓視窗內任意兩天直接互相對照，找出「關鍵日」。
  4. **GRN＋GLU 輸出**：整合成 0～1 的機率。
- **特點**：
  - 需要 PyTorch（CPU 就能訓練，但明顯比樹模型慢，分鐘級）。
  - 標籤不平衡用 BCEWithLogitsLoss(pos_weight) 自動處理。
  - 內建早停（以驗證 AUC 為準），epochs 只是上限。
  - **沒有**特徵重要性排行榜（神經網路看不到逐棵樹的用法）。
  - 最容易過擬合→防過擬合口訣：**加大 dropout → 縮小 d_model／num_layers → 縮短 seq_len**。
  - 推論端要求「最新 seq_len 個特徵完整交易日」才能組窗預測。
- **適合**：想讓模型自己學「時間上的節奏」時。

## 3-4 三款比較表

| | LightGBM | XGBoost | TFT |
|---|---|---|---|
| 白話 | 快速改錯的小樹隊 | 保守老學長 | 會看盤 30 天的 AI |
| 家族 | 梯度提升樹 | 梯度提升樹 | 深度學習（Transformer） |
| CPU 訓練速度 | 快 | 快 | 慢（分鐘級） |
| 特徵重要性 | 有（split 次數） | 有（gain 增益） | 無 |
| 易過擬合程度 | 中 | 中 | 高 |
| 複雜度旋鈕 | num_leaves | max_depth | d_model／num_layers |
| 防過擬合旋鈕 | 子抽樣比例 | 子抽樣比例 | dropout |
| 本專案預設 | CLI/API 訓練預設 | 選用對照 | 網頁前端預設 |
| 不平衡處理 | is_unbalance | scale_pos_weight | pos_weight |

---

# 四、訓練參數逐個解釋（調大／調小會怎樣）

## 4-0 調參三原則（先背這個）

- **複雜度參數**：調大＝學更細＝容易過擬合；調小＝更通用＝可能學不夠。
- **隨機性參數**（子抽樣、dropout）：調小＝越防背書；調太小＝什麼都學不到。
- **裁判只有一個**：驗證集（不是訓練集）的 AUC。任何調整都要「重訓一次→比較驗證 AUC」。

## 4-1 兩種樹模型共通參數

- **n_estimators（樹的數量）— 預設 200，允許 10～2000**
  - 是什麼：模型裡總共幾棵樹接力。
  - 調大：學更細、更慢、易過擬合。調小：學不夠（欠擬合）。
  - 小知識：本專案有早停（50 輪沒進步就停），它只是「上限」，設大常是安全的；實際停在哪由驗證集決定。
- **learning_rate（學習率）— 預設 0.05，允許 0.001～1.0**
  - 是什麼：每棵樹修正錯誤的「力道」。
  - 調大：學得快但容易衝過頭、不穩。調小：穩，但要配更多樹（n_estimators 調大）。
  - 建議：0.01～0.05 起步。

## 4-2 LightGBM 專屬

- **num_leaves（每棵樹最多幾片葉子）— 預設 31，允許 2～512**
  - 是什麼：葉子＝樹的「結論分支」。葉子越多＝規則越細＝複雜度越高。**LightGBM 最重要的複雜度旋鈕。**
  - 調大：規則更細→背資料。調小：規則粗但通用。
  - 建議：15～63，不要超過 127。
- **max_depth（樹的最大深度）— 預設 -1（不限制），允許 -1～30**
  - 是什麼：是非題最多連問幾層。-1＝不設限，交給 num_leaves 管。
  - 建議：維持 -1 或設 3～8；兩個旋鈕擇一管就好。
- **bagging_fraction（每棵樹抽多少 % 資料列）— 預設 0.8，允許 0.1～1.0**
  - 是什麼：每棵樹隨機只看 80% 的資料。
  - 調小：隨機性大、防背書；太小（<0.5）學不到東西。
  - 建議：0.7～0.9。**不要設 1.0**——重複訓練的標準差會變成「假的 0」，讓你誤判穩定性。
- **feature_fraction（每棵樹抽多少 % 特徵）— 預設 0.8，允許 0.1～1.0**
  - 是什麼：每棵樹隨機只看 80% 的特徵欄位，逼不同樹用不同線索。
  - 建議：0.6～0.8。

## 4-3 XGBoost 專屬

- **max_depth — 預設 6，允許 -1～30**
  - 是什麼：XGBoost 的主要複雜度旋鈕（它沒有 num_leaves 概念）。
  - 調大：規則深而細→易過擬合。建議：3～6（比 LightGBM 嚴格）。
- **subsample — 預設 0.8，允許 0.1～1.0**
  - 等同 LightGBM 的 bagging_fraction（每棵樹抽多少資料列）。
- **colsample_bytree — 預設 0.8，允許 0.1～1.0**
  - 等同 LightGBM 的 feature_fraction（每棵樹抽多少特徵）。

> **提醒**：調參指南裡提到的 `min_data_in_leaf`、`lambda_l2`、`gamma`、`reg_alpha`、`reg_lambda` 在本專案**未開放調整**（系統固定值），網頁上找不到是正常的，不用追。

## 4-4 TFT 專屬

- **seq_len（視窗長度）— 預設 30，允許 5～120**
  - 是什麼：每個樣本回看幾個交易日（AI 看「幾天的盤」）。
  - 調大：看更長遠、能抓中期節奏，但可用樣本變少、訓練變慢。調小：只看短線，樣本多、訓練快。
- **d_model（隱藏維度）— 預設 32，允許 8～128**
  - 是什麼：模型「腦容量」的寬度。**TFT 最容易過擬合的旋鈕。**
  - 調大：表達力強但明顯易背書、訓練慢。建議：8～32 起步，驗證 AUC 撐不起來再加大。
- **n_heads（注意力頭數）— 預設 4，允許 1～8**
  - 是什麼：幾支「不同顏色的螢光筆」同時畫重點。
  - **注意：必須能整除 d_model**（例如 d_model=32 配 4 頭 OK；d_model=30 配 4 會直接報錯）。
- **num_layers（LSTM 層數）— 預設 2，允許 1～6**
  - 是什麼：記憶模組疊幾層。調大：抓深層節奏，但易過擬合、CPU 明顯變慢。
- **dropout — 預設 0.1，允許 0～0.5**
  - 是什麼：訓練時隨機關掉這個比例的神經元（防背書）。**TFT 最重要的正則化旋鈕。**
  - 訓練 AUC 與驗證 AUC 分岔（越走越開）時，第一個動作就是調大它。0＝不丟棄（不建議）；0.2～0.3 常見。
- **learning_rate — 預設 0.003，允許 0.001～1.0**
  - AdamW 優化器的學習率。太大＝loss 震盪不收斂；太小＝收斂太慢。
- **epochs（訓練輪數上限）— 預設 30，允許 3～200**
  - 是什麼：全部訓練資料完整讀幾遍的「上限」。
  - 放心設大：TFT 內建早停（驗證 AUC 連續數輪沒進步就停），不會真的跑滿。
- **batch_size（批次大小）— 預設 128，允許 16～512**
  - 是什麼：每次修正權重前，一次餵幾個視窗樣本。一般不動它。

## 4-5 訓練設定（網頁上其他選項，不是模型超參數）

- **標籤版本（label_version）**：定義「什麼叫好買點」的規則（見 4-6）。預設「進階雙關卡」。
- **重複訓練次數（n_repeats）**：1～10。設 1 只有一組數字；設 3～5 才有「平均 ± 標準差」可判斷穩定性。
- **滾動視窗（lookback_years）**：預設 3（只用最近 3 年）；0＝全部歷史。太舊的市場規則可能過時，滾動視窗讓模型專心學「近代」。
- **訓練納入最新資料（include_latest）**：預設勾選。評估指標仍用原切分誠實計算，但最終上線模型會用「訓練＋驗證全部資料」重擬合一次→學到最新行情。
- **進場門檻（決策看板用）**：預測機率 > 門檻才顯示買點。調高＝訊號少而精；調低＝訊號多而雜。
- **系統內建防護**（不能改，但要知道）：
  - 樹模型早停 50 輪（`EARLY_STOPPING_ROUNDS = 50`）。
  - 單次訓練 30 分鐘上限（`TRAIN_TIMEOUT_SECONDS = 1800`），超時自動停並記錄在 notes。
  - 訓練資料至少 30 筆（`MIN_TRAINING_ROWS = 30`）才肯開訓。
  - 所有超參數都有允許範圍（`HYPERPARAM_BOUNDS`），輸入超範圍會被前後端擋下。

## 4-6 預測目標 Y 與 32 個特徵 X（快速複習）

- **Y（Return20）**＝(第 20 個交易日的還原收盤價 − 今天還原收盤價) ÷ 今天還原收盤價。
  - **MVP 單門檻**：Return20 > 3% → 標籤 1，否則 0。
  - **進階雙關卡（預設）**：①贏過大盤（個股 Return20 − 大盤 Return20 > 3%）**且** ②過程不停損（20 日內最低價不曾跌破今天收盤 × 95%）→ 標籤 1。
  - 為什麼用雙關卡：單看漲幅會把「先暴漲後崩跌」也當好買點；加上停損關卡＝「贏大盤且拿得住」才算。
- **X（32 個特徵）**分類（每個英文名稱後面的括號是中文意思；想看更完整的白話解釋可回 1-2～1-6 搜尋）：
  - 技術面（4）：ma20_slope（20日均線斜率）、ma60_slope（60日均線斜率）、rsi（相對強弱指標）、macd（MACD動能柱狀圖）
  - 籌碼面（2）：institutional_net_buy_ratio（法人買賣超占近20日成交量比）、margin_usage_ratio（資券使用率）
  - 相對大盤（2）：excess_return_vs_benchmark（相對大盤超額報酬）、beta_vs_benchmark（相對大盤Beta值）
  - 跨市場總經（4）：usd_twd_change_rate（匯率月變動率）、sox_change_rate（費半月變動率）、vix_level_percentile（VIX歷史百分位）、vix_change_rate（VIX月變動率）
  - 籌碼面進階（3）：inst_net_buy_20d（外資+投信近20日累計買賣超）、inst_accel（籌碼加速度）、margin_mom（融資餘額月變化率）
  - 技術面進階（7）：bias_ma20（股價對20日均線乖離率）、bias_ma60（股價對60日均線乖離率）、vol_ratio_5_20（量能比：5日均量÷20日均量）、volatility_20d（近20日年化波動率）、rsi_diff_3d（RSI近3日變化量）、rsi_monthly（月線RSI）、macd_monthly（月線MACD）
  - 跨市場進階（4）：sox_mom（費半20日累積漲跌幅）、rs_vs_sox（個股對費半相對強弱）、usd_twd（美元兌台幣匯率）、vix（VIX恐慌指數）
  - ETF 折溢價（4，僅 ETF 有值）：etf_premium_discount（當日折溢價率）、etf_premium_ma5（折溢價5日平均）、etf_premium_z20（折溢價20日z-score）、etf_premium_chg_5d（折溢價5日變化）
  - 月頻營收（2）：revenue_mom（營收月增率）、revenue_yoy（營收年增率）
- **缺值處理**：整欄皆空的特徵自動剔除（如 ETF 沒營收）；零星缺值整列捨棄（保守策略）。

---

# 五、怎麼判斷「這些數字有沒有參考價值」

## 5-1 驗證集 AUC 對照表（最重要的一張表）

| 驗證 AUC | 白話解讀 |
|---|---|
| 0.50 | 擲硬幣，毫無預測力 |
| 0.50～0.55 | 幾乎沒用，跟猜的差不多 |
| 0.55～0.60 | 有微弱但真實的預測力——**股票資料的現實水準**，本專案以 0.55 當「有用」門檻 |
| 0.60～0.70 | 不錯，值得重視 |
| 0.70 以上 | 股市中非常稀有——**先開心前先檢查**：是否偷看未來／資料洩漏／驗證集太像訓練集 |

- **訓練 AUC vs 驗證 AUC 差距 < 0.05**：健康。
- **訓練 0.90／驗證 0.55**：嚴重過擬合（考古題 95 分、考卷不及格）。
- **多次訓練間驗證 AUC 波動 ±0.03 內**：穩定。

## 5-2 PR-AUC 怎麼看

- 基準線＝標籤 1 的佔比。例如資料中 20% 是好買點，PR-AUC 至少要 > 0.20 才有參考價值，0.3 以上算不錯。
- AUC 普普但 PR-AUC 很慘＝模型只會「排先後」，不會「抓出少數好樣本」。

## 5-3 平均 ± 標準差（n_repeats 的成績）

- 0.58 ± 0.01：很穩，可信。
- 0.58 ± 0.05：有點看運氣。
- 0.58 ± 0.10 以上：靠運氣——
  - 樹模型：調整子抽樣設定、降低複雜度（num_leaves／max_depth）。
  - TFT：**調大 dropout**（變異來自隨機初始化／dropout／批次順序，加大正則化是正解）。

## 5-4 Walk-Forward 穩定性卡（首頁）

- 本專案判定「穩定性：足」的條件：至少 2 輪回測、平均 AUC ≥ 0.55、最遠一輪相對最初一輪衰退 ≤ 0.05。
- **參考價值百分比**＝AUC ≥ 0.55 的輪數佔全部輪數的比例。80%＝大部分時期都有用。
- 第一輪很好、之後每況愈下＝模型「老化」，需要重訓或加大正則化。

## 5-5 特徵重要性（Top 20 長條圖）

- 健康：重要性分散在多個特徵（技術面、籌碼面、跨市場都有）。
- 危險：**單一特徵佔比 ≥ 35%**——首頁會自動跳出過擬合警告（模型可能背特定模式，而非學通用規律）。
- 演算法：LightGBM＝split（特徵被用來切幾次）；XGBoost＝gain（貢獻多少分）；TFT 沒有這張圖。

## 5-6 樣本數與其他細節

- 樣本太少（例如 < 500 筆）的 AUC 多半是運氣；本專案最低門檻 30 筆只是「能跑」，不是「可信」。
- 驗證集若只有一種類別（全是 0 或全是 1），AUC 會記為 None——這是誠實不硬算，不是系統壞掉。
- 有疑問先看訓練紀錄的 **notes** 欄（早停位置、特殊狀況都記在這）。

## 5-7 對症下藥速查表（看到什麼 → 調什麼）

| 症狀 | 第一優先 | 其次 |
|---|---|---|
| 訓練高、驗證低（過擬合） | 樹模型：調小 num_leaves／max_depth | 調高子抽樣比例、調低 learning_rate |
| 訓練、驗證都低（欠擬合） | 樹模型：調大 n_estimators | 稍增 num_leaves／max_depth |
| TFT 過擬合 | 調大 dropout | 縮小 d_model／num_layers |
| AUC 標準差大（不穩） | TFT：調大 dropout | 樹模型：調整子抽樣、n_repeats 設 3～5 看分布 |
| 樣本數太少 | 縮短 seq_len 或 lookback_years | 多支股票合併訓練 |
| 上線（盲測）衰退 | 一鍵重訓練 | 加大正則化、縮短滾動視窗 |

## 5-8 訓練循環怎麼走（AUC → SHAP → 修正 → 重訓 → 再比對）

```
訓練 → 算驗證 AUC／PR-AUC（± 標準差）→ 跑 SHAP／置換重要性診斷
   │
   ├─ 沒有誤導特徵 ──────────► 收工：存模型、留報告（下次的「情況 A／B」基準）
   └─ 有誤導特徵 ─► 依類型修正：剔除獨大／加單調約束／分箱平滑（人工）
                          │
                          └────────► 用相同切分「重訓」──► 回到第二格，再比一次
```

- **每次調整的唯一裁判是驗證集**：訓練集分數再高都不算數；「驗證集錯誤率最低、且與訓練集差距最小」才是最佳參數。
- **三個數字三種意義**：AUC 管「排名的能力」、PR-AUC 管「少數好樣本的召回」、SHAP 管「信不信得過」——要一起看。
- **可以收工的訊號**：驗證 AUC ≥ 0.55、訓練與驗證差距 < 0.05、± 標準差小、SHAP 不再抓出誤導特徵。
- **需要調整的訊號**：驗證 < 0.55 或差距 > 0.05（→查 5-7 對症下藥）＋ SHAP 出現獨大（≥35%）／方向悖離／依賴曲線鋸齒（→查 2-5）。
- 診斷與重訓指令：`python manage.py diagnose_model <代碼> --model-type lightgbm|xgboost|tft --auto --retrain`；完整報告自動存回 `ModelTrainingRun.diagnostics`（Admin 可查）。**SHAP 診斷已納入訓練流程**：每次訓練完成後會自動診斷（超參數 `auto_shap=false` 可關閉），手動指令用於歷史舊紀錄補診斷或帶修正重訓比對。

## 5-9 數字是怎麼算出來的（AUC 與 SHAP 的誕生過程）

> 這一節回答兩個最常被問的問題：「AUC 這個數字是哪來的？」「SHAP 這個數字是哪來的？」
> 程式碼位置：AUC 在 `apps/stocks/model_training.py`（641-668 行附近）、SHAP 在 `apps/stocks/diagnostics.py`（180-231 行附近）。

### 5-9-1 AUC＝考試成績（模型的「排序能力」）

**第 1 步：先有正確答案（y 標籤）**。每一天都有一個標籤（定義見 `X_Y_VARIABLE_DEFINITION.md`）：預設「進階雙關卡」——未來 20 個交易日報酬贏過大盤 3% **且** 過程最低價不曾跌破起算日收盤 ×95% → 標籤 1（好買點），否則 0。

**第 2 步：模型給每個日子打分數**。訓練完成後，把**驗證段（20% 那段時間）**每一天的特徵餵給模型，得到每天的「機率」。

**第 3 步：算 AUC**（`roc_auc_score(y_val, y_proba)`）。直白解釋：**隨機抽一個「真漲（標籤 1）」的日子和一個「沒漲（標籤 0）」的日子，模型給真漲那天分數比較高的機率**。

- 0.5＝瞎猜（亂排）；1.0＝完美排序；本專案的「有用」門檻是 **0.55**（對照表見 5-1）
- 它衡量的是**排序能力**，跟「買進門檻 50%」無關——所以 AUC 0.59 的模型，某天給台積電 37.30%（低於門檻）依然合理：AUC 說「它的排名大致靠譜」，37.30% 說「今天不是它認為的好買點」

**網頁上出現的幾種 AUC，來源各不相同：**

| 出現位置 | 怎麼算 |
|---|---|
| 訓練紀錄表「AUC ± 標準差」 | 同一設定重複訓練 `n_repeats` 次（隨機性不同），**每次各算一個驗證 AUC**，顯示平均值±樣本標準差。存檔的模型挑「AUC 最接近平均」那次，避免存到運氣特別好的一次 |
| Walk-Forward 卡每輪 AUC／PR-AUC | 另一套演練：訓練窗逐年擴大、驗證窗逐年後滑，**每輪各自算一次**驗證 AUC／PR-AUC（見 5-4） |
| SHAP 診斷卡「重訓對比」 | 帶修正重訓後的新模型 AUC vs 原模型 AUC，看修正有沒有幫助（見 2-5 情況 A／B） |

**PR-AUC 補充**（`average_precision_score`）：在「好買點很少見」（正例稀少）時比 AUC 更嚴格誠實——它的瞎猜基準線不是 0.5，而是「正例佔比」（見 5-2）。

### 5-9-2 SHAP＝答卷分析（模型「每天靠誰做判斷」）

**SHAP 值本身**（樹模型用 TreeSHAP：`shap.TreeExplainer(model).shap_values(X)`）：對**驗證段**的每一天、每一個特徵，算出一個貢獻值——「這個特徵在這一天，把模型輸出的機率往上推或往下拉了多少」。有嚴格的加總恆等性：**某天所有特徵的 SHAP 值加起來＝那天的預測 − 基準預測**。例：基準（全驗證段平均）38%，某天模型輸出 42%，其中 macd 的 SHAP＝+3.5%，代表 macd 這天的值把判斷往上推了 3.5 個百分點。

**卡片上的兩個數字：**

- `mean(|SHAP|)`＝該特徵在驗證段所有日子的貢獻取絕對值後平均（不看正負、只看**影響力大小**）
- `share`（佔比）＝該特徵的 mean(\|SHAP\|) ÷ 全部特徵總和 → 全部加起來＝100%。例如「beta_vs_benchmark 佔 14.16%」＝模型判斷的功勞裡它分到 14.16%

**TFT 的數字不是 SHAP**（神經網路吃不了 TreeExplainer）：改用**置換重要性**——把驗證段某個特徵逐股打亂（保留分布、破壞與答案的對應），重跑推論看 **AUC 掉多少**（`auc_drop`）；掉越多＝模型越依賴它。卡片會誠實標注兩種單位不同：「mean(\|SHAP\|)」vs「AUC 降幅（置換後）」。

**三種「誤導證據」的判定**（詳見 2-5）：

| 類型 | 判定 |
|---|---|
| 獨大/作弊 | 某特徵 share ≥ 35% |
| 方向悖離 | 特徵值與 SHAP 值的 Spearman 相關違反領域直覺（p<0.05 才報） |
| 鋸齒 | 前 12 大特徵的 SHAP 依賴曲線震盪指數 ≥ 0.5（疑似硬背噪訊） |

### 5-9-3 兩張「重要性」卡片，數字很像但基準完全不同

| 卡片 | 數字意義 | 來源 |
|---|---|---|
| 特徵重要性 Top 20 | LightGBM＝**split**：特徵在整片森林裡**被拿來切幾次**（例：margin_usage_ratio 20 次／全體 150 次＝13.33%）；XGBoost＝**gain** 增益 | 樹模型原生統計，訓練完就記錄 |
| SHAP 誤導特徵診斷卡 | **對預測結果的實際貢獻**佔比（mean(\|SHAP\|)），或 TFT 的 AUC 降幅 | 訓練完成後自動診斷算出來（`auto_shap=false` 可關閉） |

**一句話總結**：**AUC 是「考試成績」（排序能力，用驗證段考出來），SHAP 是「答卷分析」（每題靠哪個知識點拿分、有沒有在硬背）**——成績好不代表沒作弊，所以兩張卡要一起看。

---

# 六、網頁畫面上會看到的詞

## 6-1 首頁 SOP 五步（標準作業流程）

1. **檢查是否有新資料**：比對資料源與資料庫，看有沒有新行情。
2. **同步**：跑爬蟲把新資料抓進資料庫（FinMind＝台股個股行情／籌碼／營收；yfinance＝匯率與 ^TWII／^SOX／^VIX 指數）。
3. **建立特徵**：把原始資料加工成 32 個特徵。
4. **建立標籤**：算出每列資料的「標準答案」（Return20 規則）。
5. **訓練**：讓模型上課。
- **一鍵重訓練**：同步→特徵→標籤→用「最新 60/20/20」自動切分訓練，一次按完。
- **自選日期一鍵訓練**：同上，但四個日期完全由你指定（訓練起訖＋驗證起訖）。
- **dry_run（預覽模式）**：只顯示「將會做什麼」，不真的執行。

## 6-2 訓練紀錄表格與模型檔

- **AUC／PR-AUC 平均 ± 標準差**：n_repeats > 1 才會有 ± 標準差。
- **彩色狀態徽章**：訓練完成／失敗的狀態標籤。
- **訓練時間（台北）**：訓練紀錄表的時間欄一律以台北時間（UTC+8）顯示——資料庫統一存 UTC，後端回傳前自動轉換。
- **SHAP 診斷欄**：訓練紀錄表的「SHAP 診斷」欄顯示每次訓練的診斷狀態——已診斷的紀錄會附上誤導證據數（獨大／方向／鋸齒）與最大特徵佔比；空白「—」代表該紀錄尚未診斷（多半是納入自動診斷之前的歷史紀錄，可手動補做）。
- **本次訓練資訊卡（model-info）**：首頁顯示「這次訓練會用到什麼資料與參數」，隨模型下拉即時切換。
- **特徵重要性 Top 20 卡**：最近一次完成的樹模型訓練的重要性長條圖（LightGBM＝split 使用次數、XGBoost＝gain 增益）；單一特徵佔比 ≥ 35% 顯示過擬合警告。
- **Walk-Forward 穩定性評估卡**：依模型類型彙整每輪 AUC／PR-AUC，輸出「穩定性：足／不足」與「參考價值百分比」；有做過診斷的輪次會附 SHAP 證據摘要。
- **SHAP 誤導特徵診斷卡**：顯示最近一次診斷報告——SHAP 重要性排行、三類誤導證據（獨大／方向悖離／鋸齒）、修正建議與重訓後新舊 AUC 對比。**診斷已納入訓練流程**：每次訓練完成後自動執行（`auto_shap=false` 可關閉），歷史舊紀錄可手動補做。
- **trained_models/*.joblib**：訓練好的模型存檔。檔名格式＝「模型_訓練起_訓練迄_val_驗證起_驗證迄_（r＋重訓時間戳）」，例如 `v_tft_2022-08-28_2025-01-20_val_2025-01-21_2026-08-27_r20260827030246.joblib`——一眼可看出這顆模型是用哪段資料練的。

## 6-3 決策看板（個股詳細頁）

- **K 線＋均線＋AI 買賣點**：主圖。模型機率超過門檻的日子會標記買點。
- **AI 推論結果卡（六種狀態）**：右上角指標卡會依「最新機率 × 門檻 × 模擬持倉狀態」顯示六種建議——新觸發建倉 (BUY)、維持觀望 (WAIT)、持倉觀察中 (HOLD)、賣出訊號觸發 (SELL)、建議停利賣出 (TAKE PROFIT)、建議停損賣出 (STOP LOSS)；卡片同時顯示預測機率、門檻與「歷史模擬買點／賣點次數」。
- **停利／停損（模擬）**：歷史買賣點模擬自最近一次模擬買點起算——報酬達 **+6% 停利**或 **−4.5% 停損**就記一個賣點（強制出場）。這是「模型訊號的模擬部位」，進場價＝買訊當天收盤價；**系統沒有儲存你的實際持有成本**，卡片建議針對模擬部位，不是你的真實持股。
- **動態突破機率 vs 門檻**：模型每天輸出的機率畫成線，與你設的進場門檻線比較。
- **進場門檻**：右欄「策略設定」可即時調整，改了圖表立即重算。調高（如 0.7）＝少而精；調低（如 0.5）＝多而雜。
- **特徵監控／即時因子監控**：把某個特徵的歷史值畫出來，看「模型在看的線索」現在什麼狀況。
- **AI 預測**：讓「該架構最近一次訓練完成」的模型對最新資料實際跑一次推論。

## 6-4 相關性分析（/analysis/）

- **Pearson 相關係數**：兩組數字的「直線」同向程度（−1～+1）。
- **Spearman 相關係數**：改用「排名」算的相關，比較不怕極端值。
- **Lagged（落後期）相關**：今天 A、N 天後 B 的相關→找「誰領先誰」（例如 VIX 今天高，台股 5 天後容易跌？）。
- **Rolling（滾動）相關**：固定窗口邊滑邊算→看兩者關係近年變強還是變弱。
- **p 值**：< 0.05＝大致不是巧合。

---

# 七、一頁速查（CHEAT SHEET）

- **預測目標**：20 個交易日後贏大盤 3% 且不停損 → 1，否則 0。
- **資料切分**：時間序 60% 訓練／20% 驗證／20% 盲測。
- **穩的證據**：驗證 AUC ≥ 0.55、訓練驗證差距 < 0.05、± 標準差小、多輪回測都 ≥ 0.55。
- **不穩的證據**：訓練 0.9 驗證 0.55、單一特徵重要性 ≥ 35%、回測第二輪以後大幅衰退。
- **保守起手**：LightGBM 用預設（num_leaves 31、learning_rate 0.05、n_estimators 200）；TFT 用預設（d_model 32、dropout 0.1、epochs 30）。先用預設跑一輪，再依 5-7 對症下藥。
- **TFT 過擬合口訣**：先調大 dropout → 再縮 d_model／num_layers → 最後縮 seq_len。
- **樹模型過擬合口訣**：先縮 num_leaves／max_depth → 調子抽樣 → 降 learning_rate 配大 n_estimators。
- **調參三原則**：讓模型簡單、增加隨機性、加入正則化。
- **SHAP 黃金循環**：訓練 → AUC／PR-AUC → SHAP 診斷 → 有誤導就修正重訓 → 再比對；樹用 TreeSHAP、TFT 用置換重要性。**診斷已納入訓練流程**（訓練完成後自動執行，`auto_shap=false` 可關閉）；`python manage.py diagnose_model` 用於歷史舊紀錄補診斷或帶修正重訓比對。

---

# 附註：與其他文件的關係

- `README.md`：專案總覽、安裝步驟、各 App 現況、特徵計算式。
- `MODEL_TUNING_GUIDE.md`：調參與防過擬合的完整指南（進階）。
- `X_Y_VARIABLE_DEFINITION.md`：X／Y 的權威定義（缺值規則、時間安全）。
- 誤導特徵診斷（2-5／5-8）對應 `python manage.py diagnose_model`（`apps/stocks/diagnostics.py`）；報告存回 `ModelTrainingRun.diagnostics`。
- 本文件＝上述文件的「白話翻譯版」。若發現數字不一致，以程式碼 `apps/stocks/model_training.py` 為準。
