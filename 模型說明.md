# 模型說明.md — 三種模型、實際資料與特徵的訓練與篩選（白話版）

> 本檔由原「特徵與模型.md」與「模型說明.md」合併而成，涵蓋：訓練流程、三種模型、特徵篩選機制。
> 對應程式碼：`apps/stocks/model_training.py`（訓練主流程）、`apps/stocks/tft_model.py`（TFT）、`apps/stocks/diagnostics.py`（誤導特徵診斷）。
> 特徵清單權威定義：`X_Y_VARIABLE_DEFINITION.md`。

---

## 一句話先講結論

**不是「單一特徵丟進某個參數」。** 本專案是把「某天、某支股票」的 **32 個特徵數值排成一列，加上那天的標準答案（標籤）**，組成一行一行像 Excel 的表格，然後**整張表餵給模型**，模型一次看所有特徵、所有資料，自己學出「什麼樣的組合之後會漲」。

---

## 第 1 步：先把資料整理成「一張大表格」

訓練前的資料長這樣（**每一列 = 一支股票在一個交易日的快照**）：

```
日期         股票  │ ma20_slope  rsi   macd  法人買超比  vix  ...  │ 標籤(label)
───────────────────┼────────────────────────────────────────────┼──────────
2024-01-02   2330  │   0.12      55    1.3     +2.1     18   ...  │    1
2024-01-02   0050  │   0.08      48    0.7     +0.3     18   ...  │    0
2024-01-03   2330  │   0.15      58    1.5     +3.4     19   ...  │    1
  ...（所有股票 × 所有交易日，幾千~幾萬列）
```

- **中間那 32 欄 = 特徵（X）**：技術面 4 + 籌碼面 2 + 相對大盤 2 + 跨市場 4 + 進階籌碼 3 + 進階技術 7 + 進階跨市場 4 + ETF 折溢價 4 + 月營收 2
- **最後那 1 欄 = 標籤（Y，標準答案）**：`Return20 > 3%` 就記 1（好買點），否則記 0。也就是「從這天買進、放 20 個交易日，有沒有賺超過 3%」

```
標籤怎麼來：
股價   t ●━━━━━━━━━━ 20個交易日 ━━━━━━━━● t+20
             Return20 = (P(t+20) − P(t)) / P(t)
             > 3% → 標籤 = 1（而且進階版還要求中間不跌破 95% 停損線）
             否則 → 標籤 = 0
```

這張表就是「**題目（32 個特徵）＋解答（標籤）**」的題庫。

---

## 第 2 步：把題庫切成「課本、模擬考、聯考」

資料**按時間順序**切三段（不能隨機切，不然等於考卷洩題）：

```
時間軸 ──────────────────────────────────────────────────►
[━━━━━━ 60% 訓練集 ━━━━━━][━━ 20% 驗證集 ━━][━━ 20% 盲測集 ━━]
     模型上課讀的課本         模擬考            聯考
   模型看這段「學規律」    每輪課後小考      訓練完才准看
                          （決定何時停課、   （最誠實的成績）
                           調整參數）
```

程式碼對應：`_load_dataset(train_start, train_end)` 抓課本、`_load_dataset(validation_start, validation_end)` 抓模擬考卷。

---

## 第 3 步：三個模型怎麼「吃」這張表（重點！）

### ① LightGBM 和 ② XGBoost（樹模型）— 像「會問是非題的醫生」

每一列（某天某股票的 32 個數字）**一起**丟進模型。模型內部建幾百棵「決策樹」，每棵樹就是一串是非題：

```
一筆樣本：{rsi=55, macd=1.3, 籌碼=+2.1, vix=18, 營收MoM=5%, ...共32個}

        【第 1 棵樹】
          rsi < 70 ?
          /       \
        是         否
        │           \
    籌碼>0?       vix > 25?
     /    \         ...
   是      否
   ↓       ↓
  +0.3分  -0.1分   ← 葉子上有分數

  → 第2棵樹專門修正第1棵樹答錯的地方 → 第3棵修正前兩棵…共 200 棵
  → 全部分數加總 = 這筆樣本「之後會漲」的機率
```

- `n_estimators=200`：最多建 200 棵樹
- `learning_rate=0.05`：每棵樹只能小幅修正，避免矯枉過正
- `is_unbalance=True`：因為「會漲」的樣本本來就少，自動加重它的分數
- LightGBM 和 XGBoost 原理幾乎一樣，只是樹的長法不同（一個往寬長、一個往深長）

**訓練時在做什麼？** 電腦不斷掃過課本所有題目，自動找出「切在哪個特徵的哪個數值，能把 1 和 0 分得最開」的是非題。32 個特徵**全部同時參與**，模型自己決定哪個特徵重要（事後會輸出「特徵重要性排名」）。

### ③ TFT（Transformer）— 像「看 30 天走勢圖的技術分析師」

這是唯一不同的：樹模型看「單日一張快照」，TFT 看「**連續 30 天的走勢**」：

```
樹模型：  [某一天的32個數字] ──────────────► 機率

TFT：     [第1天] [第2天] ... [第30天]     ← 30天 × 32個特徵
             │      │            │           排成一張小時間表
             ▼      ▼            ▼
          ┌─────────────────────────┐
          │ 1. 特徵篩選層(VSN)：哪幾個特徵重要 │
          │ 2. LSTM：讀「時間順序」的變化     │
          │ 3. 注意力機制：30天裡哪幾天最關鍵  │
          └─────────────────────────┘
                        ▼
                     輸出機率
          （這個視窗的答案 = 第30天那格的標籤）
```

- `seq_len=30`：每個樣本是 30 個交易日的視窗
- `epochs=30`：整套題庫輪播最多 30 遍（樹模型沒有這個概念，TFT 是慢慢疊代修正）
- `batch_size=128`：一次抓 128 個視窗算一輪修正
- 驗證集 AUC 連續沒進步就提前停課（早停）

---

## 第 4 步：訓練中的防作弊與收尾

訓練過程實際會做的幾件事（對應程式碼）：

| 機制 | 白話 |
|---|---|
| **早停**（`EARLY_STOPPING_ROUNDS=50`） | 模擬考成績 50 輪沒進步就停課，防止「背課本」（過擬合） |
| **重複訓練**（n_repeats） | 換不同的隨機種子多練幾次，報告寫「AUC=0.58±0.02」→ ±越小越穩 |
| **存哪一次？** | 存「成績最接近平均」的那一次，不存運氣最好那次 |
| **月營收用公告日** | 不允許模型偷看「還沒公布」的營收（look-ahead bias 防護） |
| **標籤不平衡** | 會漲的樣本少，三種模型都自動加權，不用人工補資料 |

訓練完模型存成 `.joblib` 檔（`trained_models/` 資料夾），之後「進行 AI 預測」就是：抓**今天最新**的 32 個特徵 → 餵進模型 → 吐出「之後 20 天漲過 3% 的機率」。

---

## 補充：32 個特徵不是每次都全部進場

候選是 32 個，實際進訓練的是 **26～30 個**（純個股 28、純 ETF 30、混合 26）——完整的篩選機制見文末〈**附錄：特徵是怎麼篩選出來的？**〉。

---

## 用一個比喻總結整條流程

> 把**幾萬張「股票體檢報告」**（每張 = 某天某股票的 32 項檢查數值）收集起來，每張後面貼一張「這個人 20 天後健康還是變差」的結果貼紙。
> - **LightGBM/XGBoost**＝一位醫生看完所有報告，學出一套「如果 RSI 這樣、籌碼那樣，就傾向會漲」的判斷流程圖
> - **TFT**＝另一位醫生不只看單張報告，而是把**連續 30 天的報告疊起來看變化趨勢**再下判斷
> - 訓練＝醫生讀 60% 的舊報告練判斷；驗證＝用沒讀過的 20% 考他；盲測＝最後 20% 完全沒碰過的期末考

---

## 附錄：特徵是怎麼篩選出來的？

### 先分清楚兩件事：「一開始的 32 個」和「每次訓練實際用的」

1. **32 個特徵是「人」設計的，不是電腦自己選的。**
   人類依領域知識設計出 9 大分類（技術面、籌碼面、相對大盤、跨市場總經、進階籌碼、進階技術、進階跨市場、ETF 折溢價、月營收），由 `python manage.py build_features` 計算後存進資料庫（日頻 30 個存 `StockFeatureDaily`、月頻 2 個存 `StockFeatureMonthly`）。程式碼裡的 `FEATURE_COLUMNS` 只是「**候選名單**」。

2. **每次訓練實際用哪幾個，由兩道自動篩選決定**（下面說明）。

### 32 個候選的分組：共通 26 ＋ 僅個股 2 ＋ 僅 ETF 4

| 分組 | 特徵 | 數量 |
|---|---|---|
| **共通**（個股、ETF 都算得出，每次都會進訓練） | 技術面 4（`ma20_slope`、`ma60_slope`、`rsi`、`macd`）＋ 籌碼面 2（`institutional_net_buy_ratio`、`margin_usage_ratio`）＋ 相對大盤 2（`excess_return_vs_benchmark`、`beta_vs_benchmark`）＋ 跨市場總經 4（`usd_twd_change_rate`、`sox_change_rate`、`vix_level_percentile`、`vix_change_rate`）＋ 籌碼進階 3（`inst_net_buy_20d`、`inst_accel`、`margin_mom`）＋ 技術進階 7（`bias_ma20`、`bias_ma60`、`vol_ratio_5_20`、`volatility_20d`、`rsi_diff_3d`、`rsi_monthly`、`macd_monthly`）＋ 跨市場進階 4（`sox_mom`、`rs_vs_sox`、`usd_twd`、`vix`） | **26** |
| **僅個股有值**（ETF 無月營收 → 整欄空） | `revenue_mom`、`revenue_yoy` | **2** |
| **僅 ETF 有值**（個股無淨值 → 整欄空） | `etf_premium_discount`、`etf_premium_ma5`、`etf_premium_z20`、`etf_premium_chg_5d` | **4** |

26＋2＋4＝32。

### 第一道篩選：組資料時「整欄算不出來的自動剔除」

對應 `_load_dataset`，訓練開始前逐一檢查 32 欄：

```
候選 32 個
   │  _load_dataset 逐一檢查：「這次訓練勾的股票中，
   │  有任一檔這個特徵『整欄皆空』嗎？」
   │      ├─ 個股沒有 ETF 淨值 → 4 個折溢價整欄空 → 剔除
   │      └─ ETF 沒有月營收  → 2 個營收整欄空   → 剔除
   ▼
實際進訓練 26~30 個（純 ETF 30 個、純個股 28 個、混合 26 個）
   │  剩下的「零星缺值」（例如滾動窗口天數還沒滿的早期資料）
   ▼  整「列」丟棄（保守策略：寧可少一筆資料，也不要編數字塞進去）
```

- 這一道是**結構性篩選**：剔除的原因是「這種標的根本沒有這項資料」，跟模型表現好壞無關。
- 混合個股＋ETF 訓練時取「所有股票都算得出來」的特徵交集，兩種標的的樣本才都能留下來。

**三種訓練組合實測**（資料庫真實紀錄驗證：讀取每筆 `ModelTrainingRun.feature_importance` 與 `FEATURE_COLUMNS`（32 個）做差集，實測剔除清單與下表完全吻合）：

| 訓練標的 | 實測進訓練特徵數 | 被剔除 | 對應規則 |
|---|---|---|---|
| **純個股** 2330（2 筆訓練） | **28 個** | 4 個 ETF 折溢價 | 個股算不出折溢價 |
| **純 ETF** 0050（1 筆訓練） | **30 個** | 2 個月營收 | ETF 沒有月營收 |
| **混合** 0050+2330（12 筆訓練，含 run #166 `lightgbm_autoshap_e2e`） | **26 個** | 4 折溢價＋2 營收 | 交集：兩邊各有一組算不出 → 兩組都剔除 |

### 第二道篩選：訓練完的「誤導特徵診斷」可能剔除（或加約束）

對應 `diagnostics.py` 的 SHAP 誤導特徵診斷。**訓練完成後自動**對「驗證區間」跑診斷（`attach_auto_diagnosis`），找三類證據：

| 證據類型 | 判斷標準（程式門檻） | 白話意思 | 修正動作 |
|---|---|---|---|
| **獨大／作弊特徵** | 單一特徵重要性佔比 ≥ **35%**（`LEAK_SHARE`） | 有一個特徵佔了快一半的判斷依據，很像在背答案／洩漏 | **剔除後重訓** |
| **方向悖離領域直覺** | SHAP 方向與 `MONO_HINTS` 相反且統計顯著 | 例如「VIX 越恐慌越好買點」違反常識 | **加單調約束**（+1/−1，不是剔除） |
| **依賴曲線鋸齒狀** | 震盪指數 ≥ **0.5**（`JAGGED_OI`） | 特徵值小小變動、答案就大上大下＝在硬背噪訊 | 無法自動修，人工建議分箱／平滑／對數轉換 |

診斷方法依模型能力不同：

- **LightGBM／XGBoost**：用 TreeSHAP（精確、快），看每個特徵對預測的貢獻方向與大小
- **TFT**：吃不了 TreeSHAP，改用「置換重要性」——把驗證段某個特徵的值**打亂**，看 AUC 掉多少；掉越多＝模型越依賴它

兩個重要的行為細節：

- **自動診斷只診斷、不重訓**：報告存進 `ModelTrainingRun.diagnostics` 供人查看；要「帶著剔除清單重訓」需執行 `python manage.py diagnose_model --auto --retrain`，剔除清單（`exclude_features`）和約束（`monotone_constraints`）會注入該次訓練的 `run_training`。
- **剔除只影響「那一次訓練」**：不改變 `FEATURE_COLUMNS` 這份 32 個的權威定義。

另外，方向健檢只檢查「方向近乎公理」的特徵（如法人買超＝越大越好、VIX＝越高越危險）；RSI、乖離率、匯率這類「非單調或方向不明」的特徵**不做方向健檢、也不硬加約束**——硬約束反而傷害模型。

### 第三個概念：模型內部本來就會「軟性」挑特徵

上面兩道是「整欄剔除」；其實模型在訓練時還有一層**隱性篩選**：

- **樹模型**：每個節點都從所有特徵中挑「最能分辨 0／1」的來切是非題 → 沒用的特徵自然幾乎沒被用到（重要性趨近 0）、有用的特徵一直被切（重要性高）。這就是為什麼訓練完有「**特徵重要性 Top 20**」可以看。
- **TFT**：內建的 Variable Selection Network（VSN）會對每個時間步算出各特徵的權重（softmax 總和＝1），**軟性**決定這一步要聽哪個特徵多一點。

所以完整答案是：**「篩選」發生在三個層次**——人設計候選清單 → 系統剔除算不出的欄 → 診斷剔除誤導的欄；而模型內部自己還會決定「每個特徵要用多少」。

### 兩個常見問句的直接回答

- **32 個特徵都會進入模型訓練嗎？**
  → 不會全部進。候選是 32 個，實際進訓練的是 **26～30 個，依這次訓練勾選的標的組合而定**（混合訓練最少、純 ETF 最多）。
- **「哪些特徵是個股用、哪些是 ETF 用？」**
  → **沒有任何特徵是「只給某一類用」的專屬設計**——ETF 折溢價 4 個只有 ETF 算得出來、月營收 2 個只有個股算得出來，其餘 26 個兩種都會進訓練。

### 一圖總結篩選流程

```
① 人類設計候選          ② 結構性篩選              ③ 誤導診斷（訓練後）
   32 個候選    ────►   整欄皆空剔除    ────►   26~30 個進訓練 ────► 診斷報告
（領域知識＋            （個股無折溢價、          │    ▲                 │
 build_features）        ETF 無營收）            │    └─ 獨大(≥35%)剔除重訓 ◄┘
                                                 ▼
                                        模型內部自行加權
                                     （樹：重要性／TFT：VSN 權重）
```

